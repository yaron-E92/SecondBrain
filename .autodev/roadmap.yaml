version: 1
priority:
  # Product-correction wave. #138 is the governing SecondBrain provenance contract;
  # generic AutoDev provenance enforcement is tracked by yaron-E92/AutoDev#335.
  # #138 is therefore not itself scheduled as an application implementation issue.
  # UX/presentation work is not complete from CI/code inspection alone: run the
  # named user journey and compare artifact-covered states against the pinned UX
  # authority, with unavailable provenance layers reported as not verified.
  - issue: 125

  # Repair the highest-friction lifecycle/navigation journeys first: focused
  # context creation, in-place Home creation, then clear Browse/workspace/item
  # boundaries. These establish stable destinations before richer data is shown.
  - issue: 134
  - issue: 135
  - issue: 137

  # Enrich the Core model deliberately, then expose those semantics through
  # complete type-specific editing and restrained collection/workspace summaries.
  # Home remains primary placement; secondary associations are typed relations.
  - issue: 139
  - issue: 140
  - issue: 136
  - issue: 141

  # Journey: capture an outcome before every task is known. Workstreams are the
  # semantic/portfolio container; workflow dependencies stay in Streamline and
  # executable scheduling/recurrence stays in ShuffleTask. #143 has an explicit
  # UX-design gap governed by #138 and must not copy task/workflow state into Core.
  - issue: 143

  # Finish the existing Notion migration journey against the corrected product
  # model. Import success includes visible preservation/deferment of supported
  # metadata and relations, not merely row creation.
  - issue: 43

  # Standalone SecondBrain value and data safety come before ecosystem breadth.
  # These journeys build on the corrected navigation/settings composition.
  - issue: 68
  - issue: 69
  - issue: 66
  - issue: 67

  # Understand, persist, enforce, and expose optional-module consent safely.
  - issue: 45
  - issue: 46
  - issue: 47
  - issue: 48

  # Harden Notion vocabulary after the import/product semantics are stable.
  # Preserve immutable mapping-version meaning and expose shipped-vs-user alias
  # provenance rather than turning aliases into semantic remapping.
  - issue: 129

  # Opt into ShuffleTask, see useful work, open it, and return while preserving
  # independent product shapes and a real package boundary.
  - issue: 49
  - issue: 114
  - issue: 91
  - issue: 50
  - issue: 51
  - issue: 52
  - issue: 53

  # Search Core and enabled modules with source ownership, restrained metadata,
  # canonical deep links, and safe partial-provider degradation.
  - issue: 54
  - issue: 55
  - issue: 56
  - issue: 57
  - issue: 58

  # Journey: review the portfolio, understand which outcomes can move, which are
  # waiting, and where to continue. #144 consumes source-owned external summaries
  # only after the relevant provider/navigation foundations above exist; it never
  # recomputes Streamline readiness or ShuffleTask scheduling inside Core.
  - issue: 144

  # Find PHOODAB-owned information and preview replenishment with zero side effects.
  - issue: 59
  - issue: 60
  - issue: 61
  - issue: 62
  - issue: 63

  # Add the minimal confirmed cross-module command boundary after a real preview
  # consumer exists. Core reminder/retry value is intentionally delivered earlier.
  - issue: 64
  - issue: 65

  # Ask private cited questions and execute only explicitly confirmed proposals.
  - issue: 71
  - issue: 72
  - issue: 73
  - issue: 74
  - issue: 75
  - issue: 76

  # Publish a provenance-backed installable release once higher-value product work
  # is unavailable or complete.
  - issue: 111

# Intentionally omitted from active priority:
# - #138: governing SecondBrain UX-provenance contract; generic engine enforcement
#   is AutoDev#335, and #138 should be validated through subsequent UX work rather
#   than treated as a standalone application feature.
# - #70: its own contract defers sync research until a concrete sync consumer exists.
# - #92-#94: tracker issues; implementation readiness belongs to their child issues.
# - completed foundational issues such as #100-#103 and #102.
fallback: oldest
